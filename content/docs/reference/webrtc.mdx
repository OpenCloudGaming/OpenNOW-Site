---
title: Protocol notes (historical WebRTC)
description: Historical Chromium/WebRTC signaling notes; not the default Qt desktop path
---

import { Callout } from 'fumadocs-ui/components/callout';
import DataChannelsTable from '@/components/docs/DataChannelsTable';

<Callout type="warn" title="Not the default desktop path">
OpenNOW's Qt desktop client streams through the in-process native NVST runtime. It does **not** embed Chromium, does not use renderer `RTCPeerConnection`, and has no WebRTC fallback. This page keeps historical Electron-era signaling notes that can still help when reading older issues or comparing protocol concepts. For current desktop behavior, use [Native Streamer](/docs/reference/native-streamer/).
</Callout>

## Historical Electron dual-path model

The retired Electron client had two streaming clients that shared main-process session lifecycle and NVST signaling:

- **Web mode** (`streamClientMode: "web"`, default in Electron): the renderer owned Chromium `RTCPeerConnection`, media elements, data channels, stats, microphone, screenshots, and recordings.
- **Native mode** (experimental child process): a Rust/GStreamer process answered the server offer. Electron could fall back to web mode when native checks failed.

That dual-path design is gone from the Qt app.

## Signaling shape (still useful context)

Historical OpenNOW desktop builds connected to the session signaling server like this:

```text
wss://{signalingServer}/nvst/sign_in?peer_id={name}&version=2
```

- Subprotocol: `x-nv-sessionid.{sessionId}`
- Origin: `https://play.geforcenow.com`
- Heartbeat: `{ hb: 1 }` every 5 seconds

After the `peer_info` init message, incoming signaling messages were classified as SDP offers, ICE candidates, or peer messages that may need ACKs.

## Historical renderer peer connection

In Electron web mode, the renderer:

1. Fixed server SDP placeholders such as `0.0.0.0` using the session server IP.
2. Created `RTCPeerConnection` with GFN ICE servers.
3. Preferred the configured codec and normalized color quality.
4. Set the remote offer and created a local answer.
5. Waited for ICE gathering, then sent the answer plus NVST SDP.
6. Injected the manual media endpoint candidate from `mediaConnectionInfo` when present.
7. Attached video/audio tracks to `<video>` and `<audio>` elements.

## NVST SDP attributes

OpenNOW historically sent standard WebRTC SDP plus an NVIDIA NVST SDP blob. NVST SDP carries stream-quality attributes such as resolution, viewport, FPS, bitrate, codec, color quality, QoS/FEC, bandwidth estimation, and ICE credentials.

| m-line | Direction | Purpose |
| --- | --- | --- |
| `video` | receive-only | Cloud GPU video RTP |
| `audio` | receive-only | Game audio |
| `mic` | send-only | Microphone audio when enabled |
| `application` | bidirectional | Input data channels |

## Data channels

<DataChannelsTable />

The partially reliable threshold was read from the negotiated `a=ri.partialReliableThresholdMs` SDP attribute.

## Where to go instead

| Need | Page |
| --- | --- |
| Current desktop streaming path | [Native Streamer](/docs/reference/native-streamer/) |
| Process boundaries | [Architecture](/docs/architecture/overview/) |
| Settings and local data | [Configuration](/docs/reference/configuration/) |
| Black screen / decode isolation | [Troubleshooting](/docs/guides/troubleshooting/) |
