---
title: Native Streamer Architecture
description: Advanced notes on the in-process Qt/native NVST streamer path
---

import NativeEnvironmentVariablesTable from '@/components/docs/NativeEnvironmentVariablesTable';
import NativePlatformVideoPathsTable from '@/components/docs/NativePlatformVideoPathsTable';

OpenNOW's desktop client streams through an in-process native NVST runtime loaded by Qt. This is not an experimental Electron child process, and there is no Chromium/WebRTC fallback. Report streaming issues on [GitHub Issues](https://github.com/OpenCloudGaming/OpenNOW/issues) or [Discord](https://discord.gg/8EJYaJcNfD) with a diagnostics export when possible.

## Shipped design

```text
GFN CloudMatch session ── Rust core ── session context ── Qt shell
                                              │
                                              └─ opennow-streamer-ffi (in-process)
                                                       │
                                                       └─ NVST transport + platform decode
                                                              │
                                                              └─ GPU texture → Qt scene graph
```

The Rust core owns authentication and CloudMatch session lifecycle. Qt owns windows, overlays, and presentation. The embedded streamer owns NVST sockets, media, input, recording, and GPU frame publication. Qt does not create a second video window or spawn a streamer child process.

## Responsibilities

| Component | Owns |
| --- | --- |
| Rust core | CloudMatch, settings, catalog, updates, diagnostics export helpers |
| Qt shell | UI, overlays, QRhi device lending, desktop/console layouts |
| Native streamer FFI | NVST lifecycle, decode/audio/input/recording, GPU frame tokens |

## Platform decode/render selection

<NativePlatformVideoPathsTable variant="preferred" />

`nativeVideoBackend` defaults to `auto`. Unsupported forced backends fail explicitly rather than silently selecting a different API.

## Packaging

Qt CMake builds `opennow-streamer-ffi` and installs it beside the executable and `opennow-core`. Look for `OpenNOW-Qt-…` packages. Linux packages may include a bundled FFmpeg fallback while GPU driver interfaces remain dynamically discovered.

## Environment overrides

<NativeEnvironmentVariablesTable variant="advanced" />

## Diagnostics

Native and Qt logs are collected through **Settings → About → Copy diagnostics**. Distinguish an entirely black application window from a stream-only black video surface when filing reports.

## Known limitations

- The Qt app is still under active development; platform-specific streaming and GPU issues are expected.
- Publishing a build does not mean every acceptance checklist item has passed.
- Nightly packages may be unsigned/not notarized.
- Separate Android, iOS, Switch, and OpenNOW-Mac projects are not this Qt desktop path.

For the full reference, use [Native Streamer](/docs/reference/native-streamer/). Historical Chromium notes remain on [Protocol notes](/docs/reference/webrtc/).
